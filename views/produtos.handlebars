<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Meus Produtos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1>Meus Produtos</h1>
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">Produtos Cadastrados:</h5>
        <p class="card-text">Escova de dente: {{productName}}</p>
        <p class="card-text">Arroz: {{productName}}</p>
        <p class="card-text">Feijão: {{productName}}</p>        
      </div>
    </div>
  </div>
</body>
</html>
